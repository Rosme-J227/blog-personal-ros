{
  "name": "merorosdays-blog",
  "version": "1.0.0",
  "description": "MeroRosDay's — Blog / Muro interactivo personal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx -y gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.117.1"
  }
}
